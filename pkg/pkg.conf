# Beta OS package manager configuration
# OpenBSD-current amd64 package repository
REPOSITORY=https://cdn.openbsd.org/pub/OpenBSD/snapshots/packages/amd64/
ARCH=amd64
CHANNEL=current
FORMAT=txz
SIGNATURES=required

# Packages are verified before installation.
# The repository is intentionally pinned to OpenBSD snapshots/current.
