name: Build Beta OS x86_64

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc binutils make nasm grub-pc-bin grub-common xorriso mtools qemu-system-x86

      - name: Create persistent Beta OS disk
        run: dd if=/dev/zero of=build/beta-os-disk.img bs=1M count=16 status=none

      - name: Build x86_64 kernel and ISO
        run: make

      - name: Smoke test kernel with persistent disk
        run: |
          timeout 8s qemu-system-x86_64 -cdrom build/beta-os.iso -drive file=build/beta-os-disk.img,format=raw,if=ide -display none -serial none || test $? -eq 124

      - name: Upload Beta OS ISO
        uses: actions/upload-artifact@v4
        with:
          name: beta-os-x86_64-iso
          path: build/beta-os.iso

      - name: Upload persistent disk image
        uses: actions/upload-artifact@v4
        with:
          name: beta-os-x86_64-disk
          path: build/beta-os-disk.img

      - name: Upload kernel ELF
        uses: actions/upload-artifact@v4
        with:
          name: beta-os-x86_64-kernel
          path: build/kernel.elf
